"""Cairn on-device extraction model toolkit.

Public entry points live in the submodules and are wired up as the CLI scripts
declared in ``pyproject.toml``. Importing the package itself is cheap and does
not pull torch / transformers.
"""

from __future__ import annotations

__version__ = "0.1.0"
